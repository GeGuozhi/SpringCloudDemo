spring:
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://gitee.com/fzggz/springcloud
          searchPaths: spring-cloud-config-file
          timeout: 60
          username: 14702954341
          password: gitee9527...ggz
          default-label: newBranch
          force-pull: true  # 强制拉取最新代码
          refresh-rate: 0   # 禁用缓存，每次都从Git读取

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/