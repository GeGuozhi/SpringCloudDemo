<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.psbc.invres.resarchlibrary.mapper.doris.OdsdbMapper">

    <select id="searchBranches" resultType="java.util.LinkedHashMap">
        SELECT branch, CONCAT(branch, '分行') AS branchValue
        FROM dim_org
    </select>

</mapper>
