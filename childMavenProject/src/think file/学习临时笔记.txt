学习目标


分布式技术教育行业以及互联网经验优先岗位描述：
	1.深入挖掘和分析业务需求，撰写技术方案和系统设计，确保系统的架构质量；
	2.系统核心部分代码编写，疑难问题的解决；
	3.维护和升级现有软件产品和系统，快速定位并修复现有软件缺陷；
	4.能为团队引入创新的技术、创新的解决方案，用创新的思路解决问题，能对现存或未来系统进行宏观的思考，规划形成统一的框架、平台或组件的优先。

要求：
	1.JAVA基础扎实，理解nio、多线程、集合等基础框架，对JVM原理有一定的了解；
	2.1年及以上使用JAVA开发的经验，对于你用过的开源框架，能了解到它的原理和机制；对SpringMVC,MyBatis,SpringBoot等开源框架熟悉；
	3.熟悉分布式系统的设计和应用，熟悉分布式、缓存、消息等机制；能对分布式常用技术进行合理应用，解决问题；
	4.掌握多线程及高性能的设计与编码及性能调优；有高并发应用开发经验；
	5.掌握Linux操作系统和大型数据库（Oracle、MySql）；对sql优化有丰富的经验



动态代码使用学习下？

二叉查找树，中序，升序排列。

幻读

优化。

RU 太蠢了这个，读取别人没有提交的数据。
RC 存在，不可重复读，原因，a事务统计过程中，b事务
RR mysql 默认隔离级别，加锁解决幻读现象。


InvocationHandler invocationHandler = new ? implenments InvocationHandler(Person.class);
Proxy.newProxyInstance(classloader,interfaces,invocationHandler);
动态代理生成的代理类已经继承了proxy，因为java不能多继承的原因，所以只能代理接口，
如果是类的话，必须extends 这个类，但是多继承不符合java语法，因此只能是接口。

byte short int long
double float

boolean 

char

hashmap 线程不安全，可以使用hashtable 但是不能为空，size 默认 11，扩容为2n+1。


不可重复读，指的是，一个事务中两次读取数据不一样。
解决，read repeated,通过设置删除，修改版本控制。

幻读，指的是，根据条件更新的时候，由于其他事务对当前条件中的数据进行了修改，会导致更新多或者更新少的情况出现。

请求到token，保护后的服务都需要token正确且有效才可以。feign请求


用户从认证中心拿到token，然后持久化到库里面。
通过feign访问其他被oauth 正则匹配保护的 服务的时候，可以带上token即可。

增加oauth保护，端口地址增加保护。

将token，cliendId保存到redis，设置过期时间

序列化，将对象转化成可以传输和存储的形式
反：解码，转化成对象的过程。



实现序列化接口serializable接口


1. get field 从内存中获取count
2. count+1
3. put field 将count+1的值刷新到内存中

4. OpenFeign在这块主要做的事情是，无感知调用http请求，把http请求封装到FeignClient里面。

5. feign在自己的客户端代码里面实现远程调用的作用。

6. zuul

